<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- Converter 등록 -->
  <conversionRule conversionWord="masking"
                  converterClass="dev.logging.logback.SensitiveDataConverter"/>

  <!-- 콘솔 -->
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss} [%thread] %-5level - %masking%n</pattern>
    </encoder>
  </appender>

  <!-- 파일 (logs/app.log) -->
  <appender name="FILE" class="ch.qos.logback.core.FileAppender">
    <file>logs/app.log</file>
    <append>true</append>

    <!-- pw 포함 로그는 파일에 기록 자체를 차단 
    	<filter class="dev.logging.logback.SecurityTaggingFilter"/>
    -->
    

    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger - %masking%n</pattern>
    </encoder>
  </appender>

  <root level="INFO">
    <appender-ref ref="STDOUT"/>
    <appender-ref ref="FILE"/>
  </root>

</configuration>
